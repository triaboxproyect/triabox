	$('#running').load('{{ path('event_next_min_sport', {'idSport': 9, 'firstResult': min_records,'maxResults': 20  }) }}');
   
   	$('#triatlon').load('{{ path('event_next_min_sport', {'idSport': 3, 'firstResult': min_records,'maxResults': 20  }) }}');

   	$('#natacion').load('{{ path('event_next_min_sport', {'idSport': 1, 'firstResult': min_records,'maxResults': 20  }) }}');

   	$('#ciclismo').load('{{ path('event_next_min_sport', {'idSport': 2	, 'firstResult': min_records,'maxResults': 20  }) }}');

   	$('#duatlon').load('{{ path('event_next_min_sport', {'idSport': 4, 'firstResult': min_records,'maxResults': 20  }) }}');

